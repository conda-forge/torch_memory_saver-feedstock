context:
  name: torch_memory_saver
  version: "0.0.9.post1"
  cuda_build_string: cuda_${{ cuda_compiler_version | version_to_buildstring }}

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/torch_memory_saver-${{ version }}.tar.gz
  sha256: 25fd4b691ed3242c3a18b2bef0dbe9de84d2e7068b96a37686a923d55c274f43

build:
  skip:
    - cuda_compiler_version == "None"
    - not linux
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  string: ${{ cuda_build_string }}py${{ python | version_to_buildstring }}h${{ hash }}_${{ build_number }}
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cuda') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib("c") }}
    - make
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - pytorch * [build=cuda*]

tests:
  - python:
      imports:
        - torch_memory_saver
      pip_check: true

about:
  homepage: https://github.com/fzyzcjy/torch_memory_saver
  summary: A PyTorch library that allows tensor memory to be temporarily released and resumed later.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - shermansiu
